apiVersion: "serving.kserve.io/v1beta1"
kind: "InferenceService"
metadata:
  name: "model"
spec:
  predictor:
    model:
      modelFormat:
        name: sklearn
      storageUri: "s3://ml-basics/test/"
      resources:
        requests:
          cpu: 500m
          memory: 1Gi